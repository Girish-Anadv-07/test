
name: Build and Deploy
on:
  push:
    branches:
      - main
permissions:
  contents: write
  id-token: write
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
        
      - name: Wiki
        run: |
          git clone https://github.com/Girish-Anadv-07/test.wiki.git
          cd test.wiki 
          git config --local user.name "${{secrets.GIT_USER}}"
          git config --local user.email "${{secrets.GIT_GMAIL}}"
          cd ..
          ls
          mv ./*.md ./test.wiki/
          cd test.wiki
          git add .
          git commit -m "update"
          git push
